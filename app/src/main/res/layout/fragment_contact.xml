<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorPrimary">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Contact Us"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:textStyle="bold" />

        <EditText android:id="@+id/contactNameEditText" android:hint="Your Name *" />
        <EditText android:id="@+id/contactEmailEditText" android:hint="Email *" android:inputType="textEmailAddress" />
        <EditText android:id="@+id/contactSubjectEditText" android:hint="Subject *" />
        <EditText android:id="@+id/contactMessageEditText" android:hint="Message *" android:minLines="4"
            android:gravity="top" android:layout_marginBottom="16dp" />
        <Button android:id="@+id/sendContactBtn" android:text="Send Message" />
    </LinearLayout>
</ScrollView>
