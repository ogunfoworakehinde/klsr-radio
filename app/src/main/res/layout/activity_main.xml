<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorPrimary">

    <!-- Toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:contentInsetStart="0dp"
            app:contentInsetEnd="0dp">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal">
                <ImageView
                    android:id="@+id/toolbarLogo"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_marginStart="12dp"
                    android:src="@mipmap/ic_launcher"
                    android:contentDescription="Logo" />
                <TextView
                    android:id="@+id/toolbarTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:text="KLSR Radio"
                    android:textColor="@android:color/white"
                    android:textSize="18sp"
                    android:textStyle="bold" />
                <View android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1" />
                <ImageButton
                    android:id="@+id/btnSettings"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginEnd="4dp"
                    android:src="@drawable/ic_settings"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:tint="@android:color/white"
                    android:contentDescription="Settings" />
            </LinearLayout>
        </androidx.appcompat.widget.Toolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Fragment container -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="120dp"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- Player bar -->
    <include
        android:id="@+id/player_bar"
        layout="@layout/layout_player_bar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="?attr/actionBarSize" />

    <!-- Custom Bottom Navigation (auto‑spaced, no fixed margin) -->
    <LinearLayout
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_gravity="bottom"
        android:background="?attr/colorPrimaryDark"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="2dp">

        <Button
            android:id="@+id/nav_home"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:text="Home"
            android:drawableTop="@drawable/ic_home"
            android:drawablePadding="2dp"
            android:background="@drawable/nav_item_bg"
            android:textColor="@drawable/nav_text_color_selector"
            android:drawableTint="@drawable/nav_text_color_selector"
            android:textSize="11sp"
            android:gravity="center"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:paddingHorizontal="2dp" />

        <Button
            android:id="@+id/nav_podcast"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:text="Podcasts"
            android:drawableTop="@drawable/ic_podcasts"
            android:drawablePadding="2dp"
            android:background="@drawable/nav_item_bg"
            android:textColor="@drawable/nav_text_color_selector"
            android:drawableTint="@drawable/nav_text_color_selector"
            android:textSize="11sp"
            android:gravity="center"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:paddingHorizontal="2dp" />

        <Button
            android:id="@+id/nav_prayer"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:text="Prayer"
            android:drawableTop="@drawable/ic_prayer"
            android:drawablePadding="2dp"
            android:background="@drawable/nav_item_bg"
            android:textColor="@drawable/nav_text_color_selector"
            android:drawableTint="@drawable/nav_text_color_selector"
            android:textSize="11sp"
            android:gravity="center"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:paddingHorizontal="2dp" />

        <Button
            android:id="@+id/nav_blog"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:text="Blog"
            android:drawableTop="@drawable/ic_article"
            android:drawablePadding="2dp"
            android:background="@drawable/nav_item_bg"
            android:textColor="@drawable/nav_text_color_selector"
            android:drawableTint="@drawable/nav_text_color_selector"
            android:textSize="11sp"
            android:gravity="center"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:paddingHorizontal="2dp" />

        <Button
            android:id="@+id/nav_more"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:text="More"
            android:drawableTop="@drawable/ic_more_horiz"
            android:drawablePadding="2dp"
            android:background="@drawable/nav_item_bg"
            android:textColor="@drawable/nav_text_color_selector"
            android:drawableTint="@drawable/nav_text_color_selector"
            android:textSize="11sp"
            android:gravity="center"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:paddingHorizontal="2dp" />
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
